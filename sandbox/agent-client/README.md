# Agent-Client

This is an Agent-Client Protocol Agent that is also an ACP client.


We want to understand if this is possible without using some kind of websockets based approach to allow the agent-client to communicate with both the client as a stdio process, and the agents downstream of it using websockets.
